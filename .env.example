# Copy this file to `.env` and restart Expo.
# Do not commit real tokens or credentials.

# Postman collection variable `baseUrl`
EXPO_PUBLIC_API_URL=http://localhost:5000/api

# Optional. Defaults to the API host without `/api` (Socket.IO shares the HTTP server).
# EXPO_PUBLIC_SOCKET_URL=http://localhost:5000
